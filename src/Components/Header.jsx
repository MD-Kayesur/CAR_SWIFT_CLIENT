 

const Header = ({title}) => {
    return (
        <div className="my-10 px-9 text-center">
            <h1 className="text-5xl font-bold">{title}</h1>
            
        </div>
    );
};

export default Header;